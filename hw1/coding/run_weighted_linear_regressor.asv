import model.regressor.LinearRegressorLocalWeight

clear %clear workspace

load X.dat
load y.dat

con = containers.Map({key1, key2, ...}, {val1, val2, ...});

myRegressor = LinearRegressorLocalWeight.train(X,y);
value = myRegressor.predict(X);

%%% plot data %%%
scatter (X, y,'g');
hold on;

plot(X, value, 'ro');
hold off;
